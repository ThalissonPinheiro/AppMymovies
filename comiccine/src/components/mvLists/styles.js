import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    horizontal: {
        display: 'flex',
        flexDirection: 'row',
    },
    testee: {
        width: 200,
        height: 300,
    },

})

export default styles;